<%_ if (data) { _%>
import { httpGet, httpPost } from '@/http/index.js'

export const <%= data.firLowName %>_api = {
  dataUrl: '/home1',
  listUrl: '/tablelist',
  listDelete: '/listDelete',
  detailUrl: '/home1'
}

export default {

  getData: (payload, config) => {
    return httpPost(<%= data.firLowName %>_api.dataUrl, payload, config)
  },
  getList: (payload, config) => {
    return httpPost(<%= data.firLowName %>_api.listUrl, payload, config)
  },
  listDelete: (payload, config) => {
    return httpGet(<%= data.firLowName %>_api.listDelete + `/{payload.id}`, payload, config)
  },
  getDetail: (payload, config) => {
    return httpGet(<%= data.firLowName %>_api.detailUrl, payload, config)
  }

}
<%_ } _%>
