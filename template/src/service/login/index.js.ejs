<%_ if (data) { _%>
import { httpGet, httpPost } from '@/http/index.js'

export const <%= data.name %>_api = {
  dataUrl: '/home1',
  listUrl: '/home1',
  detailUrl: '/home1'
}

export default {

  getData: (payload) => {
    return httpPost(<%= data.name %>_api.dataUrl, payload)
  },
  getList: (payload) => {
    return httpPost(<%= data.name %>_api.listUrl, payload)
  },
  getDetail: (payload) => {
    return httpGet(<%= data.name %>_api.detailUrl, payload)
  }

}
<%_ } _%>
