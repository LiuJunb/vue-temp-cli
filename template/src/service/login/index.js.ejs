<%_ if (data) { _%>
import { httpGet, httpPost } from '@/http/index.js'

export const <%= data.name %>_api = {
  dataUrl: '/home1',
  listUrl: '/tablelist',
  detailUrl: '/home1'
}

export default {

  getData: (payload, config) => {
    return httpPost(<%= data.name %>_api.dataUrl, payload, config)
  },
  getList: (payload, config) => {
    return httpPost(<%= data.name %>_api.listUrl, payload, config)
  },
  getDetail: (payload, config) => {
    return httpGet(<%= data.name %>_api.detailUrl, payload, config)
  }

}
<%_ } _%>
